# Pointer to const:
`const int* pointer` = `(const int)* pointer`
- This is pointer can point to any value `int` and make them const

# Const pointer:
`int* const pointer` = `int* (const pointer)`
- This is a pointer only point to 1 value
